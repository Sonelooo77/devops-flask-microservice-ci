name: First CI Pipeline

on:
    push:
        branches: [ "main" ]
    
jobs:
    test-job:
        runs-on: ubuntu-latest

        steps:

            - name: Checkout repository code
              uses: actions/checkout@v4

            - name: Run a one-line script
              run: echo "GitHub Actions | Pipeline is working."

            - name: List files in repository
              run: ls -la
